import { createContext } from "react";
export const ToDoListArrayFromContext = createContext([]);
export const openDeletDialog = createContext({});
export const openUpdatDialog = createContext({});
export const openSnackbar = createContext({});
